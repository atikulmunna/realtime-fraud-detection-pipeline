# Generated by scripts/deploy/make-secrets.sh as infra/caddy/auth.conf.
# The second field is a bcrypt hash from `caddy hash-password`, never a plaintext password.
basic_auth {
	evaluator $2a$14$replace.with.a.real.bcrypt.hash.from.caddy.hash.password
}
