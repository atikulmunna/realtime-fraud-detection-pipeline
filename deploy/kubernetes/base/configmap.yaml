apiVersion: v1
kind: ConfigMap
metadata:
  name: fraud-config
data:
  APP_ENV: production
  CREATE_DB_SCHEMA: "false"
  FEEDBACK_TOPIC: feedback
  MODEL_UPDATE_TOPIC: model-updates
  RAW_EVENTS_TOPIC: raw-events
  ANOMALIES_TOPIC: anomalies
  METRICS_TOPIC: metrics
  DLQ_TOPIC: dead-letter
  KAFKA_BOOTSTRAP_SERVERS: kafka.example.invalid:9092
  MLFLOW_TRACKING_URI: https://mlflow.example.invalid
  MODEL_DIR: /models
